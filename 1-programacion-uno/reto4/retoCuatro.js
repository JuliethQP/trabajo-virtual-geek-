'use strict';
//obtenemos el elemento con el que queremos trabajar
const titleElement=document.querySelector('.title');
const firstElement=document.querySelector('.first');
const secondElement=document.querySelector('.second');

//cambiamos su contenido con innerHTML

//titleElement.innerHTML=titleElement.innerHTML + firstElement.innerHTML;
titleElement.innerHTML=titleElement.innerHTML + secondElement.innerHTML;
